# ─────────────────────────────────────────────────────────────────────────────
# Cluster 验收链路的环境模板
#
# 用法：复制成 acceptance.env，填好真实值，然后
#   set -a; . platform/deploy/acceptance.env; set +a
#   ./platform/deploy/acceptance-cluster.sh
#
# 本文件是 `acceptance-cluster.sh` 的「必填项」契约。脚本对所有 `LUMO_TEST_*` 都
# 拒绝空值，且每一步都会断言「真的执行了活体用例」——填错值时它会红并说明，
# 而不是静默跳过。
# ─────────────────────────────────────────────────────────────────────────────
#
# ⚠️ 先读：**仓库自带的 cluster 拓扑不发布任何基础设施端口。**
#
# `compose.cluster.yml` 里只有 prometheus、dsh-web 和 11 个控制面服务（18081-18093）
# 带 `ports:`。postgres / redis / minio / nacos / milvus / opa / vault / etcd / rocketmq
# 只在 compose 网络内可达。所以下面的 `127.0.0.1:xxxxx` 在「直接 `up.sh cluster`」之后
# 是**连不上**的。
#
# **仓库已提供一份 additive 的验收覆盖文件**，把需要的端口暴露到宿主机：
#
#   LUMO_COMPOSE_EXTRA_FILES=platform/deploy/compose.cluster.acceptance.yml \
#     ./platform/deploy/up.sh cluster -d --build
#
# 它只给已有服务加 `ports:`，不新增也不删除服务，所以 preflight 与 smoke 的服务集
# 检查不受影响；端口刻意与 standalone 一致，本文件的值对两种形态都成立（代价：两个
# 拓扑不能同时起）。细节见该文件头部注释。
#
# 各服务在 compose 网络内的真实端口（取自 compose 文件的 healthcheck 与 standalone 的
# 端口映射，非猜测）：
#
#   postgres          5432        redis             6379
#   minio             9000 / 9001（console）
#   nacos             8848 / 9848（gRPC）
#   milvus            19530（服务）/ 9091（healthz）
#   opa               8181        vault             8200
#   etcd              2379        tei / tei-rerank  80
#   rocketmq          10911 / 10909 / 8081（mqproxy gRPC）
#   rocketmq-namesrv  9876
#
# 注意 `LUMO_TEST_RMQ_ENDPOINT` 要的是 **mqproxy 的 gRPC 端口**（容器内 8081），
# 不是 namesrv 的 9876/19876：v5 客户端走 gRPC，broker 必须以 `mqproxy -n` 起代理。
# 仓库里几份旧计划文档写成 `localhost:19876` 是错的。
#
# 另：`compose.standalone.yml` 本身就**发布**这些端口，所以对着 standalone 拓扑做本地
# 验证比对着 cluster 更容易（且不需要上面的覆盖文件）。

# ── 必填（脚本会拒绝空值） ───────────────────────────────────────────────────
LUMO_TEST_PG_DSN=postgres://lumo:REPLACE_ME@127.0.0.1:15432/lumo
LUMO_TEST_RMQ_ENDPOINT=127.0.0.1:8081
LUMO_TEST_NACOS_HEALTH_URL=http://127.0.0.1:18848/nacos/v1/console/health/readiness
LUMO_TEST_MILVUS_HEALTH_URL=http://127.0.0.1:19091/healthz
LUMO_TEST_OPA_HEALTH_URL=http://127.0.0.1:8181/health
LUMO_TEST_VAULT_HEALTH_URL=http://127.0.0.1:8200/v1/sys/health

# ── 可选：设了才探测 ─────────────────────────────────────────────────────────
# Nebula 是**外部**图适配器，不在任何 compose / Helm 里，`cluster-runtime.env` 与
# Helm values 都把它默认为空，knowledge 插件在空值时回落 PG 递归 CTE。所以它是可选的：
# 设了脚本会探测，不设则打印一行 INFO 后跳过。
# LUMO_TEST_NEBULA_HEALTH_URL=http://127.0.0.1:9669/status

# ── 可选：按需覆盖单个子系统的活库目标 ───────────────────────────────────────
# dsh-plugins 的活库 spec 读 `<SUBSYSTEM>_TEST_DSN`。不设时它们**回落到
# `LUMO_TEST_PG_DSN`**（桥接在 platform/vitest.setup.ts）。只有当你确实想把某个子系统
# 指到另一个库时才需要显式设置。
# SESSION_LOG_TEST_DSN=
# METERING_TEST_DSN=
# JOB_CONTROL_TEST_DSN=
# STORAGE_TEST_DSN=

# ── 可选：扩大覆盖 ───────────────────────────────────────────────────────────
# registry 的活对象存储用例读这三个；不设则那些用例 SKIP（不影响验收链的「至少一个
# 活体用例」判据，但会少一块证据）。
# LUMO_TEST_S3_ENDPOINT=127.0.0.1:19000
# LUMO_TEST_S3_ACCESS_KEY=lumo
# LUMO_TEST_S3_SECRET_KEY=REPLACE_ME

# usage-ledger 的 RocketMQ e2e 在 TestMain 里清空重建闭集 topic。不设时按 compose
# 服务标签反查正在运行的 broker；查到多个（两个拓扑同时起着）会警告并要求显式指定。
# 设 0 可关闭重置——只在明确知道 topic 历史不会拖垮套件时才这么做。
# LUMO_TEST_RMQ_CONTAINER=lumo-platform-cluster-rocketmq-1
# LUMO_TEST_RMQ_RESET=0

# ── 可选：脚本自身行为 ───────────────────────────────────────────────────────
# LUMO_ENV_FILE=platform/deploy/.env        # smoke 与 preflight 用的 compose env 文件
# LUMO_ACCEPTANCE_SKIP_SMOKE=1              # 跳过 smoke（调试时用）
# LUMO_ACCEPTANCE_TIMEOUT_SECONDS=5         # 单个健康探测的超时
# LUMO_SMOKE_TIMEOUT_SECONDS=240            # smoke 等容器就绪的总超时
