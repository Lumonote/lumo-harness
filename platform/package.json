{
  "name": "@lumo/platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "workspaces": [
    "dsh-plugins/*",
    "console"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit",
    "kb:eval": "tsx dsh-plugins/knowledge/eval/run-eval.ts",
    "desktop:dev": "cargo tauri dev --manifest-path desktop/Cargo.toml",
    "desktop:build": "cargo tauri build --manifest-path desktop/Cargo.toml"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/pg": "^8.23.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
